<form method="post" action="{{ path('app_hotel_riu_tikida_delete', {'id': hotel_riu_tikida.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hotel_riu_tikida.id) }}">
    <button class="btn">Delete</button>
</form>
